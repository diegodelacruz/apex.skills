diff --git a/apex_mcp/config.py b/apex_mcp/config.py
index 184030e..ae1c262 100644
--- a/apex_mcp/config.py
+++ b/apex_mcp/config.py
@@ -40,7 +40,6 @@ _REQUIRED_VARS: dict[str, str] = {
     "ORACLE_DB_USER": DB_USER,
     "ORACLE_DB_PASS": DB_PASS,
     "ORACLE_DSN": DB_DSN,
-    "ORACLE_WALLET_DIR": WALLET_DIR,
     "APEX_WORKSPACE_ID": _ws_id_str,
     "APEX_SCHEMA": APEX_SCHEMA,
     "APEX_WORKSPACE_NAME": WORKSPACE_NAME,
diff --git a/apex_mcp/db.py b/apex_mcp/db.py
index 871080a..b991a7f 100644
--- a/apex_mcp/db.py
+++ b/apex_mcp/db.py
@@ -81,14 +81,17 @@ class ConnectionManager:
                 except Exception:
                     pass
             _log.info("Connecting as %s@%s", user, dsn)
-            self._conn = oracledb.connect(
-                user=user,
-                password=password,
-                dsn=dsn,
-                config_dir=wallet_dir,
-                wallet_location=wallet_dir,
-                wallet_password=wallet_pass,
-            )
+            connect_kwargs = {
+                "user": user,
+                "password": password,
+                "dsn": dsn,
+            }
+            if wallet_dir:
+                connect_kwargs["config_dir"] = wallet_dir
+                connect_kwargs["wallet_location"] = wallet_dir
+                if wallet_pass:
+                    connect_kwargs["wallet_password"] = wallet_pass
+            self._conn = oracledb.connect(**connect_kwargs)
             _log.info("Connected — Oracle %s", self._conn.version)
             return f"Connected as {user}@{dsn} — Oracle {self._conn.version}"
 
diff --git a/apex_mcp/server.py b/apex_mcp/server.py
index df3127e..64a5da2 100644
--- a/apex_mcp/server.py
+++ b/apex_mcp/server.py
@@ -33,6 +33,7 @@ from fastmcp import FastMCP
 from .tools.sql_tools import apex_connect, apex_run_sql, apex_status
 from .tools.app_tools import (
     apex_list_apps,
+    apex_open_app,
     apex_create_app,
     apex_finalize_app,
     apex_delete_app,
@@ -193,8 +194,9 @@ mcp = FastMCP(
     instructions="""You are connected to an Oracle APEX 24.2 development environment via MCP.
 
 ## Lifecycle
-1. `apex_connect()` → 2. `apex_create_app(app_id, app_name)` → 3. add pages/regions/items → 4. `apex_finalize_app()`
-Always finalize. For existing apps: `apex_list_apps()` → `apex_get_app_details(id)` → inspect/update.
+**New app:** `apex_connect()` → `apex_create_app(id, name)` → add pages/regions/items → `apex_finalize_app()`
+**Existing app:** `apex_connect()` → `apex_open_app(id)` → add pages/regions/items → `apex_finalize_app()`
+Always finalize. For read-only inspection: `apex_list_apps()` → `apex_get_app_details(id)`.
 
 ## Quick Build Pattern
 ```
@@ -255,6 +257,7 @@ mcp.tool(description="Show current connection status, session state, and active
 
 # App lifecycle
 mcp.tool(description="List all APEX applications in the workspace.", annotations=_READ, tags={"app"})(apex_list_apps)
+mcp.tool(description="Open an existing APEX app for modification (add pages/components). Call apex_finalize_app() when done.", annotations=_WRITE, tags={"app"})(apex_open_app)
 mcp.tool(description="Create a new APEX app and start an import session. Call apex_finalize_app() when done.", annotations=_WRITE, tags={"app"})(apex_create_app)
 mcp.tool(description="Finalize and commit the app. Must be called after all pages/components are added.", annotations=_WRITE, tags={"app"})(apex_finalize_app)
 mcp.tool(description="Delete an APEX application permanently.", annotations=_DELETE, tags={"app"})(apex_delete_app)
diff --git a/apex_mcp/tools/app_tools.py b/apex_mcp/tools/app_tools.py
index d1c0281..e57defd 100644
--- a/apex_mcp/tools/app_tools.py
+++ b/apex_mcp/tools/app_tools.py
@@ -61,6 +61,124 @@ def apex_list_apps() -> str:
     return _json({"status": "ok", "data": rows, "count": len(rows)})
 
 
+def apex_open_app(app_id: int) -> str:
+    """Open an existing APEX application for modification (add pages, regions, items).
+
+    Starts an import session targeting the existing application without
+    recreating it.  After this call, use apex_add_page(), apex_add_region(),
+    etc. to add new components, then call apex_finalize_app() to commit.
+
+    Args:
+        app_id: Numeric application ID of an existing app (e.g., 109).
+
+    Returns:
+        JSON with status, app details, and existing page list.
+
+    Requires:
+        - Active connection (call apex_connect first)
+        - The application must already exist in the workspace
+    """
+    try:
+        validate_app_id(app_id)
+    except ValueError as e:
+        return _json({"status": "error", "error": str(e)})
+
+    if not db.is_connected():
+        return _json({"status": "error", "error": "Not connected. Call apex_connect() first."})
+
+    if session.import_begun and not session.import_ended:
+        return _json({
+            "status": "error",
+            "error": (
+                f"An import session is already active for app {session.app_id}. "
+                "Call apex_finalize_app() first, then apex_open_app()."
+            ),
+        })
+
+    try:
+        # Verify the app exists
+        rows = db.execute("""
+            SELECT application_id, application_name, pages
+              FROM apex_applications
+             WHERE application_id = :app_id
+        """, {"app_id": app_id})
+
+        if not rows:
+            return _json({
+                "status": "error",
+                "error": f"Application {app_id} not found. Use apex_list_apps() to see available apps.",
+            })
+
+        app_info = rows[0]
+        app_name = app_info.get("APPLICATION_NAME", f"App {app_id}")
+
+        # Get existing pages so session knows about them
+        page_rows = db.execute("""
+            SELECT page_id, page_name, page_mode
+              FROM apex_application_pages
+             WHERE application_id = :app_id
+             ORDER BY page_id
+        """, {"app_id": app_id})
+
+        # Reset session and ID generator
+        session.reset()
+        ids.reset()
+
+        # Start import session targeting the existing app
+        db.plsql(_blk(f"""
+wwv_flow_imp.import_begin (
+ p_version_yyyy_mm_dd=>'{APEX_VERSION_DATE}'
+,p_release=>'24.2.13'
+,p_default_workspace_id=>{WORKSPACE_ID}
+,p_default_application_id=>{app_id}
+,p_default_id_offset=>0
+,p_default_owner=>'{APEX_SCHEMA}'
+);"""))
+
+        # Update session state
+        session.app_id = app_id
+        session.app_name = app_name
+        session.workspace_id = WORKSPACE_ID
+        session.import_begun = True
+
+        # Register existing pages in session to prevent collisions
+        from ..session import PageInfo
+        for pr in page_rows:
+            pid = pr.get("PAGE_ID")
+            pname = pr.get("PAGE_NAME", "")
+            pmode = (pr.get("PAGE_MODE") or "normal").lower()
+            session.pages[pid] = PageInfo(
+                page_id=pid,
+                page_name=pname,
+                page_type=pmode,
+            )
+
+        return _json({
+            "status": "ok",
+            "app_id": app_id,
+            "app_name": app_name,
+            "existing_pages": len(page_rows),
+            "page_list": [
+                {"page_id": p.get("PAGE_ID"), "page_name": p.get("PAGE_NAME")}
+                for p in page_rows
+            ],
+            "next_step": (
+                "Import session active. Use apex_add_page() to add new pages, "
+                "apex_add_region()/apex_add_item()/etc. to add components, "
+                "then call apex_finalize_app() to commit all changes."
+            ),
+        })
+
+    except Exception as e:
+        try:
+            db.plsql("begin wwv_flow_imp.import_end(p_auto_install_sup_obj=>false); end;")
+        except Exception:
+            pass
+        session.reset()
+        ids.reset()
+        return _json({"status": "error", "error": str(e)})
+
+
 def apex_create_app(
     app_id: int,
     app_name: str,
diff --git a/apex_mcp/tools/inspect_tools.py b/apex_mcp/tools/inspect_tools.py
index c44eb08..66b4bec 100644
--- a/apex_mcp/tools/inspect_tools.py
+++ b/apex_mcp/tools/inspect_tools.py
@@ -182,7 +182,7 @@ def apex_get_page_details(app_id: int, page_id: int) -> str:
                    page_template,
                    authorization_scheme,
                    javascript_code,
-                   css_inline,
+                   NULL AS css_inline,
                    help_text,
                    reload_on_submit,
                    warn_on_unsaved_changes
@@ -397,8 +397,8 @@ def apex_list_regions(app_id: int, page_id: int) -> str:
                    condition_expression1,
                    condition_expression2,
                    region_template,
-                   template_options,
-                   parent_region
+                   region_template_options AS template_options,
+                   parent_region_name AS parent_region
               FROM apex_application_page_regions
              WHERE application_id = :app_id
                AND page_id = :page_id
@@ -682,11 +682,11 @@ def apex_update_region(
     try:
         # Verify the region exists
         existing = db.execute("""
-            SELECT id, name, plug_source
-              FROM wwv_flow_page_plugs
+            SELECT id, region_name AS name, plug_source
+              FROM APEX_240100.wwv_flow_page_plugs
              WHERE flow_id = :app_id
                AND page_id = :page_id
-               AND UPPER(name) = UPPER(:region_name)
+               AND UPPER(region_name) = UPPER(:region_name)
         """, {"app_id": app_id, "page_id": page_id, "region_name": region_name})
 
         if not existing:
@@ -705,7 +705,7 @@ def apex_update_region(
         changed: dict = {}
 
         if new_name is not None:
-            set_clauses.append("name = :new_name")
+            set_clauses.append("region_name = :new_name")
             params["new_name"] = new_name
             changed["name"] = new_name
 
@@ -720,7 +720,7 @@ def apex_update_region(
             changed["plug_source"] = "(static content updated)"
 
         if new_sequence is not None:
-            set_clauses.append("display_sequence = :new_sequence")
+            set_clauses.append("plug_display_sequence = :new_sequence")
             params["new_sequence"] = new_sequence
             changed["display_sequence"] = new_sequence
 
@@ -741,13 +741,13 @@ def apex_update_region(
 
         set_clause_str = ", ".join(set_clauses)
         update_sql = f"""
-            UPDATE wwv_flow_page_plugs
+            UPDATE APEX_240100.wwv_flow_page_plugs
                SET {set_clause_str},
                    last_updated_by = 'APEX_MCP',
                    last_updated_on = SYSDATE
              WHERE flow_id = :app_id
                AND page_id = :page_id
-               AND UPPER(name) = UPPER(:region_name)
+               AND UPPER(region_name) = UPPER(:region_name)
         """
         db.execute(update_sql, params)
         db.conn.commit()
@@ -829,7 +829,7 @@ def apex_update_item(
         # Verify the item exists
         existing = db.execute("""
             SELECT id, name, prompt
-              FROM wwv_flow_step_items
+              FROM APEX_240100.wwv_flow_step_items
              WHERE flow_id = :app_id
                AND flow_step_id = :page_id
                AND UPPER(name) = UPPER(:item_name)
@@ -877,7 +877,7 @@ def apex_update_item(
         if new_is_required is not None:
             # In APEX internals, field_required is stored as 'Y'/'N'
             req_val = "Y" if new_is_required else "N"
-            set_clauses.append("field_required = :new_is_required")
+            set_clauses.append("is_required = :new_is_required")
             params["new_is_required"] = req_val
             changed["field_required"] = req_val
 
@@ -898,7 +898,7 @@ def apex_update_item(
 
         set_clause_str = ", ".join(set_clauses)
         update_sql = f"""
-            UPDATE wwv_flow_step_items
+            UPDATE APEX_240100.wwv_flow_step_items
                SET {set_clause_str},
                    last_updated_by = 'APEX_MCP',
                    last_updated_on = SYSDATE
@@ -992,30 +992,30 @@ def apex_delete_page(app_id: int, page_id: int) -> str:
             # flow_id + flow_step_id; delete them before the parent step row.
             db.plsql(_blk(f"""
   -- Child items
-  DELETE FROM wwv_flow_step_items
+  DELETE FROM APEX_240100.wwv_flow_step_items
    WHERE flow_id = {app_id} AND flow_step_id = {page_id};
   -- Child page processes
-  DELETE FROM wwv_flow_step_processing
+  DELETE FROM APEX_240100.wwv_flow_step_processing
    WHERE flow_id = {app_id} AND flow_step_id = {page_id};
   -- Child buttons
-  DELETE FROM wwv_flow_step_buttons
+  DELETE FROM APEX_240100.wwv_flow_step_buttons
    WHERE flow_id = {app_id} AND flow_step_id = {page_id};
   -- Child dynamic actions and their acts
-  DELETE FROM wwv_flow_step_da_actions
+  DELETE FROM APEX_240100.wwv_flow_step_da_actions
    WHERE flow_id = {app_id} AND page_id = {page_id};
-  DELETE FROM wwv_flow_step_da_events
+  DELETE FROM APEX_240100.wwv_flow_step_da_events
    WHERE flow_id = {app_id} AND page_id = {page_id};
   -- Child regions (plugs)
-  DELETE FROM wwv_flow_page_plugs
+  DELETE FROM APEX_240100.wwv_flow_page_plugs
    WHERE flow_id = {app_id} AND page_id = {page_id};
   -- Child computations
-  DELETE FROM wwv_flow_page_computations
+  DELETE FROM APEX_240100.wwv_flow_page_computations
    WHERE flow_id = {app_id} AND page_id = {page_id};
   -- Child validations
-  DELETE FROM wwv_flow_step_validations
+  DELETE FROM APEX_240100.wwv_flow_step_validations
    WHERE flow_id = {app_id} AND flow_step_id = {page_id};
   -- Parent page step
-  DELETE FROM wwv_flow_steps
+  DELETE FROM APEX_240100.wwv_flow_steps
    WHERE flow_id = {app_id} AND id = {page_id};
 """))
 
@@ -1064,10 +1064,10 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
         # Look up the internal region id (plug id) from WWV_FLOW_PAGE_PLUGS
         existing = db.execute("""
             SELECT id, name
-              FROM wwv_flow_page_plugs
+              FROM APEX_240100.wwv_flow_page_plugs
              WHERE flow_id = :app_id
                AND page_id = :page_id
-               AND UPPER(name) = UPPER(:region_name)
+               AND UPPER(region_name) = UPPER(:region_name)
         """, {"app_id": app_id, "page_id": page_id, "region_name": region_name})
 
         if not existing:
@@ -1084,7 +1084,7 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
         # Count child items before deleting
         child_items = db.execute("""
             SELECT COUNT(*) AS cnt
-              FROM wwv_flow_step_items
+              FROM APEX_240100.wwv_flow_step_items
              WHERE flow_id = :app_id
                AND flow_step_id = :page_id
                AND item_plug_id = :plug_id
@@ -1096,7 +1096,7 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
 
         # Delete child items bound to this region
         db.plsql(_blk(f"""
-  DELETE FROM wwv_flow_step_items
+  DELETE FROM APEX_240100.wwv_flow_step_items
    WHERE flow_id = {app_id}
      AND flow_step_id = {page_id}
      AND item_plug_id = {plug_id};
@@ -1104,7 +1104,7 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
 
         # Delete child buttons bound to this region
         db.plsql(_blk(f"""
-  DELETE FROM wwv_flow_step_buttons
+  DELETE FROM APEX_240100.wwv_flow_step_buttons
    WHERE flow_id = {app_id}
      AND flow_step_id = {page_id}
      AND button_plug_id = {plug_id};
@@ -1112,7 +1112,7 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
 
         # Delete sub-regions (child plugs whose parent_plug_id = this plug)
         db.plsql(_blk(f"""
-  DELETE FROM wwv_flow_page_plugs
+  DELETE FROM APEX_240100.wwv_flow_page_plugs
    WHERE flow_id = {app_id}
      AND page_id = {page_id}
      AND parent_plug_id = {plug_id};
@@ -1120,7 +1120,7 @@ def apex_delete_region(app_id: int, page_id: int, region_name: str) -> str:
 
         # Delete the region itself
         db.plsql(_blk(f"""
-  DELETE FROM wwv_flow_page_plugs
+  DELETE FROM APEX_240100.wwv_flow_page_plugs
    WHERE flow_id = {app_id}
      AND page_id = {page_id}
      AND id = {plug_id};
@@ -1174,7 +1174,7 @@ def apex_delete_item(app_id: int, page_id: int, item_name: str) -> str:
     try:
         existing = db.execute("""
             SELECT id, name
-              FROM wwv_flow_step_items
+              FROM APEX_240100.wwv_flow_step_items
              WHERE flow_id = :app_id
                AND flow_step_id = :page_id
                AND UPPER(name) = UPPER(:item_name)
@@ -1192,7 +1192,7 @@ def apex_delete_item(app_id: int, page_id: int, item_name: str) -> str:
         found_name = existing[0]["NAME"]
 
         db.plsql(_blk(f"""
-DELETE FROM wwv_flow_step_items
+DELETE FROM APEX_240100.wwv_flow_step_items
  WHERE flow_id = {app_id}
    AND flow_step_id = {page_id}
    AND id = {internal_id};
@@ -1229,7 +1229,7 @@ def apex_delete_button(app_id: int, page_id: int, button_name: str) -> str:
     try:
         existing = db.execute("""
             SELECT id, button_name AS name
-              FROM wwv_flow_step_buttons
+              FROM APEX_240100.wwv_flow_step_buttons
              WHERE flow_id = :app_id
                AND flow_step_id = :page_id
                AND UPPER(button_name) = UPPER(:button_name)
@@ -1247,7 +1247,7 @@ def apex_delete_button(app_id: int, page_id: int, button_name: str) -> str:
         found_name = existing[0]["NAME"]
 
         db.plsql(_blk(f"""
-DELETE FROM wwv_flow_step_buttons
+DELETE FROM APEX_240100.wwv_flow_step_buttons
  WHERE flow_id = {app_id}
    AND flow_step_id = {page_id}
    AND id = {internal_id};
@@ -1291,7 +1291,10 @@ def apex_update_page(
     if not db.is_connected():
         return _json({"status": "error", "error": "Not connected. Call apex_connect() first."})
 
-    if not any([new_name, new_title, new_auth_scheme is not None, new_page_mode]):
+    if new_auth_scheme is not None:
+        return _json({"status": "error", "error": "APEX 24.1.3 stores page authorization outside WWV_FLOW_STEPS; use the supported export/import path for this field."})
+
+    if not any([new_name, new_title, new_page_mode]):
         return _json({
             "status": "error",
             "error": "Provide at least one field to update: new_name, new_title, new_auth_scheme, or new_page_mode.",
@@ -1301,7 +1304,7 @@ def apex_update_page(
         # Verify page exists
         existing = db.execute("""
             SELECT id, name, step_title
-              FROM wwv_flow_steps
+              FROM APEX_240100.wwv_flow_steps
              WHERE flow_id = :app_id
                AND id = :page_id
         """, {"app_id": app_id, "page_id": page_id})
@@ -1343,7 +1346,7 @@ def apex_update_page(
 
         set_clause = ",\n       ".join(set_parts)
         db.plsql(_blk(f"""
-UPDATE wwv_flow_steps
+UPDATE APEX_240100.wwv_flow_steps
    SET {set_clause}
  WHERE flow_id = {app_id}
    AND id = {page_id};
diff --git a/apex_mcp/tools/setup_tools.py b/apex_mcp/tools/setup_tools.py
index b16da05..815eb42 100644
--- a/apex_mcp/tools/setup_tools.py
+++ b/apex_mcp/tools/setup_tools.py
@@ -148,9 +148,9 @@ def apex_setup_guide() -> str:
                 "EXECUTE on WWV_FLOW_IMP_PAGE",
             ],
             "for_editing_existing_apps": [
-                "UPDATE on WWV_FLOW_PAGE_PLUGS",
-                "UPDATE on WWV_FLOW_STEP_ITEMS",
-                "DELETE on WWV_FLOW_STEPS",
+                "UPDATE on APEX_240100.WWV_FLOW_PAGE_PLUGS",
+                "UPDATE on APEX_240100.WWV_FLOW_STEP_ITEMS",
+                "DELETE on APEX_240100.WWV_FLOW_STEPS",
                 "EXECUTE on WWV_FLOW_COPY (for apex_copy_page)",
             ],
             "grant_script": """-- Run as SYS or ADMIN to grant permissions to your schema
@@ -161,8 +161,8 @@ BEGIN
 END;
 /
 -- If editing internal tables directly:
-GRANT UPDATE ON WWV_FLOW_PAGE_PLUGS TO MY_SCHEMA;
-GRANT UPDATE ON WWV_FLOW_STEP_ITEMS TO MY_SCHEMA;
+GRANT UPDATE ON APEX_240100.WWV_FLOW_PAGE_PLUGS TO MY_SCHEMA;
+GRANT UPDATE ON APEX_240100.WWV_FLOW_STEP_ITEMS TO MY_SCHEMA;
 """,
         },
         "quick_start": [
@@ -291,13 +291,13 @@ def apex_check_requirements() -> str:
                                  "User may not have APEX workspace access. Check APEX_WORKSPACE_ID."))
 
         try:
-            db.execute("SELECT 1 FROM wwv_flow_page_plugs WHERE rownum = 1")
+            db.execute("SELECT 1 FROM APEX_240100.wwv_flow_page_plugs WHERE rownum = 1")
             results.append(check("APEX internal table access", True,
                                  "Can read WWV_FLOW_PAGE_PLUGS (editing features available)"))
         except Exception as e:
             results.append(check("APEX internal table access", False,
                                  "No access to WWV_FLOW_PAGE_PLUGS",
-                                 "Editing existing apps requires: GRANT SELECT, UPDATE ON WWV_FLOW_PAGE_PLUGS TO your_schema"))
+                                 "Editing existing apps requires: GRANT SELECT, UPDATE ON APEX_240100.WWV_FLOW_PAGE_PLUGS TO your_schema"))
 
     passed = sum(1 for r in results if r["status"] == "PASS")
     failed = sum(1 for r in results if r["status"] == "FAIL")
@@ -401,7 +401,7 @@ def apex_check_permissions() -> str:
 
     # Internal tables for editing
     for table in ["WWV_FLOW_PAGE_PLUGS", "WWV_FLOW_STEP_ITEMS", "WWV_FLOW_STEPS"]:
-        ok_sel = check_select(table)
+        ok_sel = check_select(f"APEX_240100.{table}")
         ok_upd = check_dml(table, "UPDATE")
         ok_del = check_dml(table, "DELETE")
         permissions.append({
@@ -422,9 +422,9 @@ def apex_check_permissions() -> str:
         "permissions": permissions,
         "summary": f"{granted_count}/{total} permissions confirmed",
         "grant_script_if_needed": """-- Run as ADMIN/SYS to grant editing permissions:
-GRANT SELECT, UPDATE, DELETE ON WWV_FLOW_PAGE_PLUGS TO {user};
-GRANT SELECT, UPDATE, DELETE ON WWV_FLOW_STEP_ITEMS TO {user};
-GRANT SELECT, DELETE ON WWV_FLOW_STEPS TO {user};
+GRANT SELECT, UPDATE, DELETE ON APEX_240100.WWV_FLOW_PAGE_PLUGS TO {user};
+GRANT SELECT, UPDATE, DELETE ON APEX_240100.WWV_FLOW_STEP_ITEMS TO {user};
+GRANT SELECT, DELETE ON APEX_240100.WWV_FLOW_STEPS TO {user};
 -- Or more broadly for workspace schema owners (preferred):
 BEGIN
   APEX_UTIL.SET_WORKSPACE(P_WORKSPACE => 'YOUR_WORKSPACE');
@@ -469,9 +469,9 @@ def apex_fix_permissions() -> str:
         })
 
     grant_statements = [
-        f"GRANT SELECT, UPDATE, DELETE ON WWV_FLOW_PAGE_PLUGS TO {current_user}",
-        f"GRANT SELECT, UPDATE, DELETE ON WWV_FLOW_STEP_ITEMS TO {current_user}",
-        f"GRANT SELECT, DELETE ON WWV_FLOW_STEPS TO {current_user}",
+        f"GRANT SELECT, UPDATE, DELETE ON APEX_240100.WWV_FLOW_PAGE_PLUGS TO {current_user}",
+        f"GRANT SELECT, UPDATE, DELETE ON APEX_240100.WWV_FLOW_STEP_ITEMS TO {current_user}",
+        f"GRANT SELECT, DELETE ON APEX_240100.WWV_FLOW_STEPS TO {current_user}",
     ]
 
     grants_result = []
diff --git a/apex_mcp/tools/sql_tools.py b/apex_mcp/tools/sql_tools.py
index 26cac9e..e5a15ee 100644
--- a/apex_mcp/tools/sql_tools.py
+++ b/apex_mcp/tools/sql_tools.py
@@ -40,8 +40,6 @@ def apex_connect(
         ("user / ORACLE_DB_USER", user),
         ("password / ORACLE_DB_PASS", password),
         ("dsn / ORACLE_DSN", dsn),
-        ("wallet_dir / ORACLE_WALLET_DIR", wallet_dir),
-        ("wallet_password / ORACLE_WALLET_PASSWORD", wallet_password),
     ] if not val]
     if missing:
         return _json({
@@ -59,12 +57,8 @@ def apex_connect(
             wallet_dir=wallet_dir,
             wallet_pass=wallet_password,
         )
-        # Auto-discover template IDs from the live workspace (#11)
-        try:
-            from .. import templates as _tmpl
-            _tmpl.discover_template_ids(db)
-        except Exception:
-            pass
+        # Template discovery targets apex-mcp 24.2 metadata and is skipped
+        # for the direct APEX 24.1.3 compatibility profile.
         return _json({"status": "ok", "message": msg})
     except Exception as e:
         return _json({"status": "error", "error": str(e)})
