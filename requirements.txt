# MCP integration from the managed local upstream
# NOTE: This requires bootstrap - see CLAUDE.md for setup instructions
# Optional local MCP runtime. Setup-ApexSkills.ps1 installs this after upstream bootstrap.
# -e ./.upstreams/managed/apex-mcp

# Secure per-user TEST/production credential profiles
keyring>=25.0.0

# Controlled local MCP.  This is a direct dependency: it must not depend on
# the managed apex-mcp upstream, whose complete tool surface is not exposed.
fastmcp>=3.4.7,<4.0.0

# Word manual generation and embedded-image audit
python-docx>=1.1.0
Pillow>=10.0.0

# Testing and validation
pytest>=8.0.0
pytest-cov>=5.0.0

# Code quality and formatting (dev dependencies)
black==24.10.0
isort>=5.13.2
flake8>=6.1.0
bandit>=1.7.5
detect-secrets>=1.5.0
pylint>=4.0.7
mypy>=1.8.0
