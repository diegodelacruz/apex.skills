# Registro de capacidades controladas — 2026-09-17

**Estado:** **BOOTSTRAP COMPLETO; SONDEO TEST Y PRODUCCIÓN OBSERVADO**

| Actividad | Estado | Evidencia local | Riesgo, reversión y siguiente paso |
| --- | --- | --- | --- |
| 1. Bloquear registro automático del upstream | COMPLETADA | Inspección directa del wrapper y de `apex_mcp/tools/` detectó `UPDATE`/`DELETE` sobre `APEX_240100` y `wwv_flow_page_dev.delete_page`. El inicializador ya no contiene `codex mcp add`, eliminó `-RegisterProductionMcp` y separa `-ValidateOracleTest` del registro MCP. | Sólo se modificó código local. Reversión: revertir el diff dirigido. Una inscripción TEST previa no se toca y se informa como `MCP_REGISTRATION_PREEXISTING_UNVERIFIED`. |
| 2. Validar contrato y superficie MCP por lectura | COMPLETADA | `validate_apex_mcp_adapter.py` revisa archivos, `connect_kwargs`, `oracledb.connect(**connect_kwargs)` y wallet condicional, sin importar ni ejecutar el upstream. Resultado local: `MCP_ADAPTER_READY` y `MCP_SURFACE_UNSAFE`. La prueba de firma antes/después confirma que no altera archivos inspeccionados. | La validación no demuestra conectividad ni seguridad de exposición. Una futura fachada debe aplicar allowlist técnica e inventario probado antes de registro. |
| 3. Clasificar `probe` Oracle | COMPLETADA | El perfil/keyring inválido se separa de ausente e incompleto. ORA-01017/28000/28001 se clasifican como autenticación; ORA-12154/12514/12541 como conexión; lo demás y fallos posteriores a abrir sesión como sonda. La única consulta es contra `dual` con `sys_context`. | Sin sonda ejecutada en este ciclo. Reversión: revertir el diff local. TEST exige autorización explícita para conectar. |
| 4. Documentación coherente | COMPLETADA | Informe, README, compatibilidad y guía Codex MCP indican que el contrato es local, el upstream completo queda sin registro automático, APEX CRUD requiere runner auténtico y DDL requiere preflights reales. Se retiró la guía de registro manual del wrapper completo. | No hay afirmación de inicializador ni conexión remota demostrada. |
| 5. Validación local final | COMPLETADA | `pytest tests -q`: **445 passed**, cobertura **62.73%**. Validador: `MCP_ADAPTER_READY` + `MCP_SURFACE_UNSAFE`. `audit_skill_ecosystem.py`: PASS; `audit_quality_score.py`: 100/100; `git diff --check`: sin errores. | Los avisos CRLF y los cambios no relacionados ya existentes permanecen sin alterar. |
| Runner App Builder auténtico | BLOQUEADA | No existe runner autenticado ni se ejecutó una sesión APEX. | Implementar sólo con autorización y evidencia de usuario, workspace, rol, Build Status y objetivo. |
| Oracle DDL con preflights | BLOQUEADA | No se expone ejecución DDL; faltan comprobaciones reales de privilegios efectivos, cuota, dependencias, respaldo y recuperación. | Diseñar y probar preflights en TEST autorizado antes de habilitar cualquier mutación. |
| 2026-09-17 Actividad 1: sondeo automático no bloqueante | COMPLETADA | Pasos renumerados a [1/6]–[6/6] (eliminada brecha [4/7]). Producción usa `MCP_REGISTRATION_SKIPPED_UNSAFE_SURFACE` o `PREEXISTING_UNVERIFIED` igual que TEST. 450 tests pasan; cobertura 62.85%. | No registrar MCP, handshake, DDL, DML, App Builder ni modificar upstream. Reversión: revertir el diff dirigido. |
| 2026-09-17 Actividad 2: seguridad del probe | COMPLETADA | Se agregaron pruebas de `ORACLE_CONNECTION_FAIL` y `ORACLE_PROBE_FAIL` a través de la función `probe` real (no sólo `classify_connection_error`). Verificado que la salida no expone DSN, password, wallet ni driver messages. 450 tests pasan. | Reversión: revertir el diff dirigido. |
| 2026-09-17 Actividad 3: documentación operativa | COMPLETADA | Corregido CLAUDE.md (sintaxis incorrecta `set apex-mcp-test` → `set --environment test`); README.md (handshake marcado como diagnóstico); codex-desktop-mcp-oracle.md (producción usa mismo esquema de etiquetas que TEST). Búsqueda `codex mcp add.*run_apex_mcp|codex mcp remove.*apex-mcp|register.*apex-mcp`: 0 resultados. | La guía canónica describe perfiles separados, sondeo condicional y capacidades bloqueadas. Reversión: revertir el diff dirigido. |
| 2026-09-17 Actividad 4: ejecutar bootstrap | COMPLETADA | `-SkipRemoteProbe`: TEST y Producción `NOT_PROBED`, APEX `MISSING`, MCP `PREEXISTING_UNVERIFIED`. Normal: TEST `ORACLE_CONNECTION_PASS user=DDELACRUZ schema=DDELACRUZ`, Producción `ORACLE_CONNECTION_PASS user=DDELACRUZ schema=DDELACRUZ`. Upstream fingerprint idéntico antes/después (4463 archivos). Sin DDL, DML, handshake, MCP add/remove ni App Builder. | Identidades son sesión observada contra `dual`; no acreditan privilegios, cuota ni acceso APEX. |
| 2026-09-17 Actividad 5: validación final | COMPLETADA | 450 tests passed; cobertura 62.85%. `validate_apex_mcp_adapter.py`: `MCP_ADAPTER_READY` + `MCP_SURFACE_UNSAFE`. `audit_skill_ecosystem.py`: AUDIT_PASS. `audit_quality_score.py`: PASS (todos los controles). `git diff --check`: sin errores de whitespace. Grep inseguro: 0 resultados activos (1 en este registro como evidencia). | Upstream intacto. APEX CRUD, DDL y registro MCP deliberadamente bloqueados. |
